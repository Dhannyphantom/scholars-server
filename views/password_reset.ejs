<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>Reset Your Password</title>
    <!--[if mso]>
    <noscript>
      <xml><o:OfficeDocumentSettings><o:PixelsPerInch>96</o:PixelsPerInch></o:OfficeDocumentSettings></xml>
    </noscript>
    <![endif]-->
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800;900&display=swap');

      * { margin: 0; padding: 0; box-sizing: border-box; }

      body {
        font-family: 'Nunito', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        background-color: #f0f4ff;
        color: #1a1a2e;
        -webkit-font-smoothing: antialiased;
      }

      .email-wrapper {
        width: 100%;
        background-color: #f0f4ff;
        padding: 40px 16px;
      }

      .email-card {
        max-width: 520px;
        margin: 0 auto;
        background: #ffffff;
        border-radius: 24px;
        overflow: hidden;
        box-shadow: 0 8px 40px rgba(79, 70, 229, 0.12);
      }

      /* ── Header ── */
      .header {
        background: linear-gradient(135deg, #4f46e5 0%, #6366f1 50%, #818cf8 100%);
        padding: 40px 32px 48px;
        text-align: center;
        position: relative;
        overflow: hidden;
      }

      .header::before {
        content: '';
        position: absolute;
        top: -40px; right: -40px;
        width: 180px; height: 180px;
        background: rgba(255,255,255,0.08);
        border-radius: 50%;
      }

      .header::after {
        content: '';
        position: absolute;
        bottom: -20px; left: -20px;
        width: 120px; height: 120px;
        background: rgba(255,255,255,0.06);
        border-radius: 50%;
      }

      .header-logo {
        display: inline-block;
        background: rgba(255,255,255,0.2);
        border-radius: 16px;
        padding: 12px 20px;
        margin-bottom: 20px;
      }

      .header-logo span {
        font-size: 20px;
        font-weight: 900;
        color: #ffffff;
        letter-spacing: -0.5px;
      }

      .lock-icon {
        width: 72px;
        height: 72px;
        background: rgba(255,255,255,0.15);
        border: 2px solid rgba(255,255,255,0.3);
        border-radius: 50%;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 16px;
        font-size: 32px;
        line-height: 1;
      }

      .header h1 {
        font-size: 26px;
        font-weight: 800;
        color: #ffffff;
        margin-bottom: 6px;
        letter-spacing: -0.3px;
      }

      .header p {
        font-size: 15px;
        color: rgba(255,255,255,0.8);
        font-weight: 500;
      }

      /* ── Body ── */
      .body {
        padding: 36px 32px;
      }

      .greeting {
        font-size: 16px;
        color: #374151;
        margin-bottom: 12px;
        font-weight: 600;
      }

      .body-text {
        font-size: 15px;
        color: #6b7280;
        line-height: 1.7;
        margin-bottom: 28px;
      }

      /* ── OTP Block ── */
      .otp-label {
        font-size: 12px;
        font-weight: 700;
        color: #4f46e5;
        text-transform: uppercase;
        letter-spacing: 1.5px;
        text-align: center;
        margin-bottom: 12px;
      }

      .otp-container {
        background: linear-gradient(135deg, #f0f4ff 0%, #ede9fe 100%);
        border: 2px dashed #a5b4fc;
        border-radius: 16px;
        padding: 28px 24px;
        text-align: center;
        margin-bottom: 12px;
      }

      .otp-digits {
        display: inline-flex;
        gap: 8px;
        justify-content: center;
        flex-wrap: wrap;
      }

      .otp-digit {
        display: inline-block;
        width: 48px;
        height: 58px;
        background: #ffffff;
        border: 2px solid #c7d2fe;
        border-radius: 12px;
        font-size: 28px;
        font-weight: 900;
        color: #4f46e5;
        line-height: 58px;
        text-align: center;
        box-shadow: 0 4px 12px rgba(79, 70, 229, 0.12);
      }

      .otp-fallback {
        display: none; /* shown via MSO conditional below */
        font-size: 40px;
        font-weight: 900;
        color: #4f46e5;
        letter-spacing: 10px;
        text-align: center;
      }

      .otp-expiry {
        text-align: center;
        font-size: 13px;
        color: #9ca3af;
        font-weight: 600;
        margin-bottom: 28px;
      }

      .otp-expiry strong {
        color: #f59e0b;
      }

      /* ── Divider ── */
      .divider {
        height: 1px;
        background: linear-gradient(to right, transparent, #e5e7eb, transparent);
        margin: 28px 0;
      }

      /* ── Steps ── */
      .steps-title {
        font-size: 13px;
        font-weight: 700;
        color: #9ca3af;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin-bottom: 16px;
      }

      .step {
        display: flex;
        align-items: flex-start;
        gap: 14px;
        margin-bottom: 14px;
      }

      .step-num {
        flex-shrink: 0;
        width: 28px;
        height: 28px;
        background: #4f46e5;
        border-radius: 50%;
        font-size: 13px;
        font-weight: 800;
        color: #fff;
        text-align: center;
        line-height: 28px;
      }

      .step-text {
        font-size: 14px;
        color: #4b5563;
        line-height: 1.6;
        padding-top: 4px;
        font-weight: 500;
      }

      .step-text strong {
        color: #1a1a2e;
        font-weight: 700;
      }

      /* ── Warning box ── */
      .warning {
        background: #fffbeb;
        border: 1px solid #fde68a;
        border-left: 4px solid #f59e0b;
        border-radius: 12px;
        padding: 14px 16px;
        margin-top: 24px;
        font-size: 13px;
        color: #92400e;
        line-height: 1.6;
        font-weight: 500;
      }

      .warning strong {
        color: #78350f;
        font-weight: 700;
      }

      /* ── Footer ── */
      .footer {
        background: #f9fafb;
        border-top: 1px solid #f3f4f6;
        padding: 24px 32px;
        text-align: center;
      }

      .footer-app {
        font-size: 15px;
        font-weight: 800;
        color: #4f46e5;
        margin-bottom: 6px;
      }

      .footer-tagline {
        font-size: 12px;
        color: #9ca3af;
        margin-bottom: 16px;
        font-weight: 500;
      }

      .footer-links {
        margin-bottom: 16px;
      }

      .footer-links a {
        font-size: 12px;
        color: #6b7280;
        text-decoration: none;
        margin: 0 8px;
        font-weight: 600;
      }

      .footer-copy {
        font-size: 11px;
        color: #d1d5db;
        font-weight: 500;
      }

      /* ── Responsive ── */
      @media (max-width: 560px) {
        .body { padding: 28px 20px; }
        .header { padding: 32px 20px 40px; }
        .footer { padding: 20px; }
        .otp-digit { width: 40px; height: 50px; font-size: 24px; line-height: 50px; }
        .header h1 { font-size: 22px; }
      }
    </style>
  </head>

  <body>
    <div class="email-wrapper">
      <div class="email-card">

        <!-- ── HEADER ── -->
        <div class="header">
          <div class="header-logo">
            <span>🎓 <%= appName %></span>
          </div>
          <div style="margin-top: 8px;">
            <div class="lock-icon">🔐</div>
          </div>
          <h1>Password Reset</h1>
          <p>Your one-time verification code is below</p>
        </div>

        <!-- ── BODY ── -->
        <div class="body">

          <p class="greeting">Hey <%= userName %> 👋</p>
          <p class="body-text">
            We got a request to reset your <strong><%= appName %></strong> password.
            No worries — it happens to everyone! Use the code below to complete
            your reset. This code is valid for <strong><%= expiryMinutes %> minutes</strong>.
          </p>

          <!-- OTP digits -->
          <p class="otp-label">Your verification code</p>
          <div class="otp-container">
            <div class="otp-digits">
              <% const digits = String(otp).split(''); %>
              <% digits.forEach(function(d) { %>
                <span class="otp-digit"><%= d %></span>
              <% }); %>
            </div>
          </div>

          <p class="otp-expiry">
            ⏱ Expires in <strong><%= expiryMinutes %> minutes</strong>
          </p>

          <div class="divider"></div>

          <!-- Steps -->
          <p class="steps-title">How to reset your password</p>

          <div class="step">
            <span class="step-num">1</span>
            <p class="step-text">
              Open the <strong><%= appName %></strong> app and tap
              <strong>"Reset it in 60 seconds"</strong> on the login screen.
            </p>
          </div>
          <div class="step">
            <span class="step-num">2</span>
            <p class="step-text">
              Enter your email address when prompted.
            </p>
          </div>
          <div class="step">
            <span class="step-num">3</span>
            <p class="step-text">
              Enter the <strong>6-digit code</strong> above on the verification screen.
            </p>
          </div>
          <div class="step">
            <span class="step-num">4</span>
            <p class="step-text">
              Choose a strong new password and you're back in! 🎉
            </p>
          </div>

          <!-- Warning -->
          <div class="warning">
            <strong>⚠️ Didn't request this?</strong> If you didn't ask to reset your password,
            you can safely ignore this email. Your account is still secure.
            Please contact <a href="mailto:<%= supportEmail %>" style="color:#92400e;"><%= supportEmail %></a> if you're concerned.
          </div>

        </div>

        <!-- ── FOOTER ── -->
        <div class="footer">
          <p class="footer-app">🎓 <%= appName %></p>
          <p class="footer-tagline">Learning made easy, one student at a time.</p>
          <div class="footer-links">
            <a href="#">Privacy Policy</a>
            <a href="#">Terms of Service</a>
            <a href="mailto:<%= supportEmail %>">Support</a>
          </div>
          <p class="footer-copy">© <%= year %> <%= appName %>. All rights reserved.</p>
        </div>

      </div>

      <!-- Anti-phishing note -->
      <p style="text-align:center; font-size:12px; color:#9ca3af; margin-top:16px; font-family: sans-serif;">
        This email was sent by <%= appName %>. Never share your code with anyone.
      </p>
    </div>
  </body>
</html>
